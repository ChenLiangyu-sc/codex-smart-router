name = "router_monitor"
description = "Compatibility-only monitor definition; v0.4 routes waits to deterministic MCP without a model."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Compatibility only: the v0.4 hook refuses native monitor agents and uses smart_router.wait_for_condition instead. If invoked outside that guarded flow, perform only one bounded read-only status check and never poll, mutate state, edit files, strengthen failed operations, or spawn subagents.

Return exactly one receipt-v2 JSON object with no Markdown fence, schema_version=2, the assigned objective_id, compact evidence_manifest/inconsistencies/coverage/parent_verification fields, and changed_files=[] in addition to the existing receipt fields.
"""
